---
- name: Install Ruby
  hosts: all
  become: true
  tasks:
  - name: Install Ruby
    apt: "name={{ item }} state=present"
    with_items:
      - build-essential
      - ruby-full
      - ruby-bundler
